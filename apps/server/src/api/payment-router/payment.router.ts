import { Router } from "express";

const router = Router();

router.post('verify-payment')
router.post('create-new-customer')
router.post('fetch-payment-details')
router.post('process-refund')

export default router;